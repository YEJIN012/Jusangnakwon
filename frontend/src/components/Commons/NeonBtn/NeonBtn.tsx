import styles from "./NeonBtn.module.css";

const NeonBtn = () => {
  return (
    // <div className={`${styles[`container`]}`}>
    <a href="#" className={`${styles[`neon-button`]}`}>
      NEON
    </a>
    // </div>
  );
};

export default NeonBtn;
